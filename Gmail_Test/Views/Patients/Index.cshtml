@model IEnumerable<MentalHealthApp.Models.Patient>

@{
    ViewData["Title"] = "Index";
}

<h1>Patients</h1>
<p>
    <a asp-action="Create">Create New</a>
</p>

<form asp-action="Index" method="get">
    <div class="form-actions no-color">
        <p>
            <label>Find by name: <input type="text" name="SearchString" value="@ViewData["CurrentFilter"]" /></label>
            <input type="submit" value="Search" class="btn btn-default" placeholder="Please enter a Firstname or Lastname"/> |
            <a asp-action="Index">Back to Full List</a>
        </p>
    </div>
</form>

<a asp-action="Index" asp-route-sortOrder="@ViewData["NameSortParm"]"><button class="btn-primary" >Sort by Firstname</button></a>


<table class="table table-bordered table-hover table-striped">
    <thead class="table accordion-header">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
                <td>

                    <a asp-action="Edit" asp-route-id="@item.PatientId" class="btn btn-warning btn-sm">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.PatientId" class="btn btn-info btn-sm">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.PatientId" class="btn btn-danger btn-sm">Scrap Note</a>
                </td>
        </tr>
}
    </tbody>
</table>
