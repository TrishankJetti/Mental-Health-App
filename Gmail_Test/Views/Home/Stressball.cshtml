<div class="d-flex flex-column align-items-center my-5">
    <h5>Choose your squeeze speed:</h5>

    <div class="speed-options d-flex gap-4 my-3">
        <div class="speed-option">
            <div class="stress-ball fast-ball"></div>
            <small>Fast</small>
        </div>
        <div class="speed-option">
            <div class="stress-ball medium-ball"></div>
            <small>Medium</small>
        </div>
        <div class="speed-option">
            <div class="stress-ball slow-ball"></div>
            <small>Slow</small>
        </div>
    </div>

    <p class="mt-3 text-muted">Click and hold any ball to squeeze</p>
</div>

<style>
    .stress-ball {
        width: 120px;
        height: 120px;
        background-color: #4b3b6b;
        border-radius: 50%;
        box-shadow: 0 8px 15px rgba(0,0,0,0.2);
        cursor: pointer;
    }

    .fast-ball {
        transition: transform 0.1s ease;
    }

    .medium-ball {
        transition: transform 0.2s ease;
    }

    .slow-ball {
        transition: transform 0.4s ease;
    }

    .stress-ball:active {
        transform: scale(0.85);
        box-shadow: 0 4px 8px rgba(0,0,0,0.3);
    }

    .speed-option {
        text-align: center;
    }
</style>